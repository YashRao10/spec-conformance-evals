[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
ignore = ["E402"]  # per-file sys.path shims before imports are intentional

[tool.pytest.ini_options]
testpaths = ["tests"]
